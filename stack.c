#include "monty.h"

/**
 * stack_mode - A program that sets the format of the data to a stack (LIFO)
 * @stack: Pointer to the stack head
 * @line_number: Line number
 */
void stack_mode(Node **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	bus.lifi = 0;
}
